# Authentik

Identity federation / SSO layer integrated with FreeIPA via LDAP, exposing OIDC/OAuth2 and SAML to downstream apps.

## Contents (planned)
- `blueprints/` — Authentik blueprint YAML (sanitized, no secrets)
- Provider configuration notes per integrated app (PatchMon, Wazuh, OPNsense, etc.)

## TODO
- [ ] Document LDAP source config against FreeIPA
- [ ] Document each application's provider/flow setup
